A highly photorealistic, unedited screenshot of a macOS terminal application window running the PostgreSQL command-line interface `psql`. The terminal window has a dark semi-transparent background (macOS dark mode) with standard red, yellow, and green window control buttons in the top left corner. The font is a crisp, white, monospaced programming font like Menlo or Monaco. The terminal shows the following exact text sequence: first line `postgres=# CREATE DATABASE test;`, second line `CREATE DATABASE`, third line `postgres=# \c test`, fourth line `You are now connected to database "test" as user "postgres".`, fifth line `test=# CREATE USER test_user WITH PASSWORD 'qwerty';`, sixth line `CREATE ROLE`. The text is perfectly aligned, with no spelling errors, no AI artifacts, and no weird symbols. The image should look like a direct screen capture from a developer's Macbook, showing only the terminal window with a slight drop shadow against a blurred desktop background.