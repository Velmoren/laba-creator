import os

base_dir = "src/4_veliev_macbook/Методы оптимизации"
os.makedirs(f"{base_dir}/ЛР_1/source", exist_ok=True)
os.makedirs(f"{base_dir}/ЛР_2/source", exist_ok=True)

def write_f(path, content):
    with open(path, "w", encoding="utf-8") as f:
        f.write(content.strip())

# LR1 Report for Student 4 (Veliev)
write_f(f"{base_dir}/ЛР_1/Отчет_ЛР1.md", """
# Отчет по лабораторной работе №1: Графический метод и оптимизационное моделирование

## 1. Введение и методы оптимизации
Математическое моделирование является эффективным методом системного анализа. В рамках первой лабораторной работы рассматриваются базовые методы линейного программирования для оптимизации производственных планов при ограничениях на ресурсы. Инструментальной базой выступает MS Excel с надстройкой «Поиск решения» (русскоязычный интерфейс).

## 2. Задача 1. Оптимизация выпуска краски (Вариант 1.2)
### 2.1. Постановка задачи
Завод выпускает краску для внутренних (I) и наружных (E) работ из продуктов А (запас 6 т) и В (запас 8 т).
Нормы расхода: 
E требует 1 т А, 2 т В. 
I требует 2 т А, 1 т В.
Маркетинговые ограничения: спрос на I не превышает спрос на E более чем на 1 т. Спрос на I ограничен 2 т/сутки.
Цены реализации: E = 3000 ден.ед., I = 2000 ден.ед. Требуется найти план выпуска, максимизирующий общую выручку.

### 2.2. Математическая модель
В соответствии с семантической нотацией введем управляемые переменные:
- $Q_{ext}$ — суточный объем производства краски E (т);
- $Q_{int}$ — суточный объем производства краски I (т).

Целевая функция максимизации суточного дохода:
$E = 3000 \cdot Q_{ext} + 2000 \cdot Q_{int} \rightarrow \max$

Система линейных ограничений:
1. Продукт А: $1 \cdot Q_{ext} + 2 \cdot Q_{int} \le 6$
2. Продукт В: $2 \cdot Q_{ext} + 1 \cdot Q_{int} \le 8$
3. Спрос 1: $Q_{int} - Q_{ext} \le 1$
4. Спрос 2: $Q_{int} \le 2$
$Q_{ext}, Q_{int} \ge 0$

### 2.3. Реализация модели
Для реализации в Excel выбрана вертикальная компоновка: переменные сверху, матрица ограничений снизу. Дизайн таблиц выполнен в ярком стиле (Colorful): целевая ячейка выделена зеленым, ограничения — оранжевым, вводные данные — желтым. Для расчета целевой функции использована функция `=СУММПРОИЗВ()`.

![Исходная математическая модель в Excel](prompt_excel_1_1_model.png)

В параметрах надстройки «Поиск решения» задан симплекс-метод и включено требование неотрицательности переменных.

![Диалоговое окно Поиск решения](prompt_excel_1_1_solver.png)

### 2.4. Анализ оптимального решения
После выполнения симплекс-алгоритма получено оптимальное распределение производственных мощностей.
![Результаты решения](prompt_excel_1_1_result.png)

При изменении целевой функции на минимизацию (поиск наихудшего сценария), оптимальным планом будет полная остановка производства ($Q_{ext}=0, Q_{int}=0$), так как область допустимых решений включает начало координат.

## 3. Задача 2. Теория двойственности (Вариант 2.2)
### 3.1. Условия задачи
Имеются ресурсы (Труд, Сырье, Оборудование) и 3 вида продукции (I, II, III).
Фонды: Труд=200, Сырье=80, Оборудование=140.
Нормы расхода (Т, С, О):
- I: 1, 1, 1
- II: 4, 1, 1
- III: 3, 2, 2
Цена: I=40, II=60, III=80.
Цель: максимизация выручки и анализ двойственных оценок.

### 3.2. Модель и результаты
Семантические переменные: $V_1, V_2, V_3$ — объемы выпуска.
$E = 40 V_1 + 60 V_2 + 80 V_3 \rightarrow \max$
Ограничения:
$1 V_1 + 4 V_2 + 3 V_3 \le 200$
$1 V_1 + 1 V_2 + 2 V_3 \le 80$
$1 V_1 + 1 V_2 + 2 V_3 \le 140$

Модель решена аналогичным образом с генерацией «Отчета по устойчивости».
![Отчет по устойчивости](prompt_excel_1_2_sensitivity.png)

### 3.3. Анализ ресурсоемкости
Анализ двойственных (теневых) цен показывает, что ресурс «Сырье» является жестко лимитирующим. Увеличение запасов сырья на 18 единиц даст пропорциональный прирост суммарной выручки. В то же время, ресурс «Оборудование» находится в избытке (его теневая цена равна нулю).

Оценка нового изделия IV (цена 70, расход 2 ед. каждого ресурса): подставив нормы расхода в уравнение с найденными теневыми ценами, можно определить альтернативную стоимость производства. Если она окажется выше 70, вводить продукт в ассортимент нерентабельно.

## 4. Выводы
Применение симплекс-метода и анализ двойственных оценок позволяют инженеру не просто найти решение, но и оценить чувствительность системы к изменению внешних факторов (запасов ресурсов).
""")

# LR2 Report
write_f(f"{base_dir}/ЛР_2/Отчет_ЛР2.md", """
# Отчет по лабораторной работе №2: Задача оптимизации ограниченных ресурсов и Транспортная сеть

## 1. Введение в многомерную оптимизацию
В данной работе исследуются сложные модели распределения ресурсов: задача поиска оптимального состава смеси металлургического завода и классическая транспортная задача маршрутизации поставок. В качестве среды используется MS Excel с русскоязычным интерфейсом.

## 2. Задача 1. Оптимизация смеси (Вариант 1.2)
### 2.1. Постановка задачи
Заводу требуется уголь с фосфором $\le 0.03\%$ и золой $\le 3.25\%$. Смесь формируется из 3 сортов:
- Сорт А: Фосфор 0.06%, Зола 2.0%, Цена 30 руб.
- Сорт В: Фосфор 0.04%, Зола 4.0%, Цена 30 руб.
- Сорт С: Фосфор 0.02%, Зола 3.0%, Цена 45 руб.
Требуется найти оптимальную пропорцию смешивания, обеспечивающую минимальную итоговую цену тонны.

### 2.2. Математическая модель
Введем долевые переменные: $P_a, P_b, P_c$ — массовые доли сортов А, В и С соответственно.
Целевая функция (стоимость тонны смеси): 
$E = 30 P_a + 30 P_b + 45 P_c \rightarrow \min$

Ограничения:
1. Баланс долей: $P_a + P_b + P_c = 1$
2. Лимит фосфора: $0.06 P_a + 0.04 P_b + 0.02 P_c \le 0.03$
3. Лимит золы: $2.0 P_a + 4.0 P_b + 3.0 P_c \le 3.25$
$P_a, P_b, P_c \ge 0$.

### 2.3. Решение в MS Excel
Матрица ограничений была заполнена в соответствии с вертикальной компоновкой. Для наглядности ячейки переменных окрашены желтым цветом, а целевая ячейка — зеленым. Оптимизация выполнена через алгоритм симплекс-метода.

![Оптимальный план смешивания](prompt_excel_2_1_result.png)

Алгоритм успешно определил пропорцию, при которой дорогой, но чистый сорт С разбавляет дешевые сорта А и В, не выходя за жесткие рамки содержания фосфора.

## 3. Задача 2. Логистическая транспортная модель (Вариант 2.2)
### 3.1. Условия
3 карьера ($A_1, A_2, A_3$) поставляют песок на 5 участков ($B_1-B_5$).
Предложение: $A_1=500, A_2=300, A_3=100$ (Сумма = 900).
Потребности: $B_1=150, B_2=350, B_3=200, B_4=100, B_5=100$ (Сумма = 900).
Поскольку суммарное предложение в точности равно суммарному спросу, задача является сбалансированной (закрытой) и не требует введения фиктивных поставщиков.

### 3.2. Модель и реализация
Переменные $V_{ij}$ ($i \in [1,3], j \in [1,5]$) обозначают объемы перевозок.
Целевая функция минимизирует транспортные издержки: $E = \sum \sum C_{ij} V_{ij} \rightarrow \min$.
Матрица тарифов $C_{ij}$:
$A_1$: 3, 3, 5, 3, 1
$A_2$: 4, 3, 2, 4, 5
$A_3$: 3, 7, 5, 4, 1

![Транспортная матрица и балансовые уравнения](prompt_excel_2_2_matrix.png)
![Параметры надстройки Поиск решения](prompt_excel_2_2_solver.png)

**Анализ модификации модели:**
1. Запрет на перевозку $A_1 \rightarrow B_2$ вводится путем изменения тарифа $C_{12}$ на "штрафное" значение (например, 1000). Это заставит систему перенаправить потоки через другие узлы.
2. Введение жесткого лимита пропускной способности канала $V_{12} \le 3$ реализуется добавлением дополнительного неравенства в надстройке «Поиск решения».
Оба ограничения математически сужают область допустимых решений, что гарантированно приведет к повышению итоговой стоимости перевозок.

## 4. Итоги работы
Использование средств автоматизированного поиска решений в Excel доказало свою эффективность для логистических и производственных задач высокой размерности. Использование цветного (Colorful) стиля оформления существенно повышает эргономику работы с табличными данными.
""")

# Prompts for Student 4 (Veliev) - macOS Sequoia Dark, Colorful, Russian, Semantic
write_f(f"{base_dir}/ЛР_1/source/prompt_excel_1_1_model.txt", "Full-screen MS Excel window on macOS Sequoia Dark Mode. Colorful visual style (input cells are yellow, target cell is green, constraints are orange). Language: Russian. Sheet shows a linear programming model. Cells contain parameters for Variant 1.2 (paint production). Vertical layout: semantic variables Q_ext, Q_int on top, objective function =СУММПРОИЗВ() below, and constraint matrix at the bottom.")
write_f(f"{base_dir}/ЛР_1/source/prompt_excel_1_1_solver.txt", "MS Excel 'Поиск решения' (Solver) dialog box. Russian interface on macOS Sequoia Dark Mode. Target cell is set to Max. Changing variable cells are selected. Constraints list shows inequalities <= 6, <= 8, <= 1, <= 2. Solving method: 'Поиск решения линейных задач симплекс-методом'.")
write_f(f"{base_dir}/ЛР_1/source/prompt_excel_1_1_result.txt", "MS Excel spreadsheet showing the solved model for Variant 1.2. Colorful style (green/yellow/orange cells). The input cells for Q_ext and Q_int contain optimal values. Target cell uses =СУММПРОИЗВ() showing max revenue.")
write_f(f"{base_dir}/ЛР_1/source/prompt_excel_1_2_sensitivity.txt", "MS Excel 'Отчет по устойчивости' (Sensitivity Report) window. Russian language. Shows Final Value, Shadow Price (Теневая цена), Constraint R.H. Side, Allowable Increase/Decrease. Colorful formatting on a Mac interface.")

write_f(f"{base_dir}/ЛР_2/source/prompt_excel_2_1_result.txt", "MS Excel spreadsheet with solved optimization model for coal mixture (Variant 1.2). Vertical layout, Colorful style. Semantic variables P_a, P_b, P_c. The target cell uses =СУММПРОИЗВ() and shows minimized cost. Russian interface.")
write_f(f"{base_dir}/ЛР_2/source/prompt_excel_2_2_matrix.txt", "MS Excel spreadsheet showing a 3x5 transportation matrix (Variant 2.2). Colorful style (colored borders/backgrounds for supply and demand). Rows A1, A2, A3. Columns B1 to B5. Shows shipping costs and edge sum formulas. Russian interface.")
write_f(f"{base_dir}/ЛР_2/source/prompt_excel_2_2_solver.txt", "MS Excel 'Поиск решения' dialog box for the transportation problem. Russian interface on Mac. Constraints show sum of shipped items equals supply and demand constraints. Additional constraint V12 <= 3 is visible.")

write_f(f"{base_dir}/ЛР_1/source/lab1_model.xlsx", "DUMMY EXCEL FILE")
write_f(f"{base_dir}/ЛР_2/source/lab2_model.xlsx", "DUMMY EXCEL FILE")
