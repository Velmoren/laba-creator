A perfectly realistic, direct screen capture of a dark-themed Linux terminal window showing the output of a PostgreSQL `\dg` command. The prompt `test=# \dg` is visible at the top. Below it is an ASCII-formatted table with the title 'List of roles'. The table is drawn using standard ASCII characters (`-`, `+`, `|`). The column headers are 'Role name', 'Attributes', and 'Member of'. The first row under the header shows 'postgres' in the first column, 'Superuser, Create role, Create DB, Replication, Bypass RLS' in the second column, and an empty third column. The second row shows 'test_user' in the first column, with empty second and third columns. Below the table, the prompt `test=#` appears again with a blinking cursor block. The font is bright white on a matte black background, monospaced (like Ubuntu Mono), with sharp anti-aliasing. No UI elements other than the terminal content, mimicking a full-screen or cropped terminal view.