A pristine, photorealistic screenshot of a Linux terminal window (like GNOME Terminal) running a PostgreSQL session. The text is classic white on a dark purple/black background. The screen shows a database transaction:
`practice_db=# BEGIN;`
`BEGIN`
`practice_db=*# INSERT INTO stud_groups VALUES ('B-202', 1);`
`INSERT 0 1`
`practice_db=*# UPDATE scholar SET group_code = 'B-202';`
`UPDATE 3`
`practice_db=*# COMMIT;`
`COMMIT`
`practice_db=#`
The prompt explicitly shows the `*` character indicating an open transaction block. The spacing, alignment, and command output are perfectly accurate to a real PostgreSQL 15 `psql` client. No hallucinated text, just exact terminal aesthetics.