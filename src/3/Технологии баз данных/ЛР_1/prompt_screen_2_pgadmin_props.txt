A realistic screenshot of the PgAdmin 4 desktop application in Dark Mode. The interface is predominantly dark gray and charcoal. On the left side, the 'Object Explorer' tree is visible and expanded. The path is 'Servers' > 'PostgreSQL 15' > 'Databases'. The database named 'practice_db' is selected and highlighted in a subtle blue selection box. On the right side, the main panel displays the 'Properties' tab for 'practice_db'. There is a grid showing key-value pairs. The 'Oid' row has a number, the 'Database' row says 'practice_db', and the 'Owner' row says 'db_admin'. The application UI looks completely functional with authentic icons (e.g., small yellow cylinder for the database). The text is sharp, English interface, standard PgAdmin layout. Looks like a direct screen capture from a professional workstation.