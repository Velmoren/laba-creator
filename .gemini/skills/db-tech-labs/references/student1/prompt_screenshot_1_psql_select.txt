An ultra-realistic screenshot of a developer's terminal window with a dark background, showing a PostgreSQL query and its result. The terminal prompt is `test=#`. The user has typed the following SQL query on a single line: `SELECT s.name, e.score FROM students s JOIN exams e ON s.s_id = e.s_id WHERE e.c_no = 'C1';`. Right below the query, there is a neatly formatted ASCII table. The header row shows the columns `name` and `score`, separated by a vertical pipe `|`. Below the header is a separator line `----------------------+-------`. The first data row is `Иванов Иван          |     5`, the second data row is `Петров Петр          |     3`. Below the table, the text `(2 rows)` is displayed. Finally, the empty prompt `test=#` is at the bottom with a solid block cursor. The text uses a crisp, monospaced font, typical of macOS or Linux terminals. The text is perfectly readable with no distortions or hallucinated characters.