import os

base_dir = "src/2_trushkin_macbus/Методы оптимизации"
os.makedirs(f"{base_dir}/ЛР_1/source", exist_ok=True)
os.makedirs(f"{base_dir}/ЛР_2/source", exist_ok=True)

def write_f(path, content):
    with open(path, "w", encoding="utf-8") as f:
        f.write(content.strip())

# LR1 Report
write_f(f"{base_dir}/ЛР_1/Отчет_ЛР1.md", """
# Отчет по лабораторной работе №1: Оптимизационные математические модели. Графический метод решения

## 1. Введение и теоретическая база
В современной теории управления техническими и экономическими системами методы математической оптимизации играют фундаментальную роль. Принятие обоснованных управленческих решений базируется на построении адекватной математической модели исследуемого процесса. Линейное программирование (ЛП) является мощным аппаратом для решения задач оптимального распределения ограниченных ресурсов.

В данной лабораторной работе исследуются две классические задачи операционного исчисления: задача о распределении посевных площадей (графический метод) и задача об оптимальном производственном плане с использованием аппарата теории двойственности.

## 2. Задача 1. Оптимизация посевных площадей (Вариант 1.5)
### 2.1. Постановка задачи
На имеющихся у фермера 400 гектарах земли он планирует посеять кукурузу и сою. Сев и уборка кукурузы требует на каждый гектар 200 ден. ед. затрат, а сои – 100 ден. ед. Ссуда равна 60 тыс. ден. ед. Урожайность: кукуруза – 30 ц/га, соя – 60 ц/га. Цена реализации: кукуруза – 3 ден. ед./ц, соя – 6 ден. ед./ц. Максимальная вместимость склада равна 21 тыс. центнеров. Требуется найти план посева, максимизирующий выручку.

### 2.2. Математическая модель
Введем вектор управляемых переменных $\bar{x} = (x_1, x_2)$, где:
- $x_1$ — площадь посева кукурузы (га);
- $x_2$ — площадь посева сои (га).

Целевая функция (максимизация выручки от реализации урожая):
$$E(x_1, x_2) = (30 \cdot 3)x_1 + (60 \cdot 6)x_2 = 90x_1 + 360x_2 \rightarrow \max$$

Система ограничений $g_i(x_1, x_2) \le b_i$:
1. Ограничение по площади земли: $x_1 + x_2 \le 400$
2. Ограничение по финансовым затратам (ссуде): $200x_1 + 100x_2 \le 60000$
3. Ограничение по вместимости склада: $30x_1 + 60x_2 \le 21000$

Условия неотрицательности переменных: $x_1 \ge 0, x_2 \ge 0$.

### 2.3. Компьютерная реализация в MS Excel
Для автоматизированного поиска решения использована табличная модель с вертикальной компоновкой. В верхнем блоке расположены управляемые переменные $x_1$ и $x_2$. Ниже задана целевая функция, вычисляемая с помощью встроенной функции `=СУММПРОИЗВ()`. В нижнем блоке оформлена матрица ограничений. Таблица оформлена в строгом корпоративном стиле (Corporate).

![Исходная математическая модель в Excel](prompt_excel_1_1_model.png)

В надстройке «Поиск решения» была выбрана целевая ячейка, указано направление оптимизации «Максимум» и добавлен блок ограничений. Метод решения — «Поиск решения линейных задач симплекс-методом».

![Диалоговое окно Поиск решения](prompt_excel_1_1_solver.png)

### 2.4. Анализ результатов
В результате работы алгоритма получен оптимальный план.

![Результаты решения](prompt_excel_1_1_result.png)

Если решать данную задачу на минимум, то оптимальным планом будет $x_1 = 0, x_2 = 0$, так как ограничения имеют вид «меньше или равно», и точка начала координат входит в область допустимых решений. Минимальная выручка при этом составит 0 ден. ед.

## 3. Задача 2. Теория двойственности (Вариант 2.5)
### 3.1. Постановка задачи
Предприятие выпускает 3 вида продукции (I, II, III), используя Труд, Сырье 1, Сырье 2 и Оборудование.
Прибыль: I=6, II=10, III=9.
Необходимо максимизировать прибыль и провести анализ двойственных оценок.

### 3.2. Математическая модель
Вектор переменных $\bar{x} = (x_1, x_2, x_3)$ — объемы выпуска изделий I, II и III.
Целевая функция: $E = 6x_1 + 10x_2 + 9x_3 \rightarrow \max$

Ограничения:
$3x_1 + 6x_2 + 4x_3 \le 2000$ (Труд)
$20x_1 + 15x_2 + 20x_3 \le 15000$ (Сырье 1)
$10x_1 + 15x_2 + 20x_3 \le 7400$ (Сырье 2)
$0x_1 + 3x_2 + 5x_3 \le 1500$ (Оборудование)
$x_1, x_2, x_3 \ge 0$

### 3.3. Реализация и анализ
Задача решена с использованием `=СУММПРОИЗВ()` в вертикальной компоновке. Для анализа сгенерирован «Отчет по устойчивости».

![Отчет по устойчивости](prompt_excel_1_2_sensitivity.png)

Из анализа отчета следует, что ресурс «Труд» является дефицитным (его теневая цена больше нуля). Увеличение запаса труда на 24 единицы приведет к увеличению общей прибыли предприятия пропорционально теневой цене данного ресурса.

Оценка изделия IV (цена 11, расход 8, 4, 20, 6): подставив данные нормы расхода в уравнение с теневыми ценами ресурсов, мы вычисляем альтернативную стоимость изделия. Если альтернативная стоимость выше цены реализации (11), выпуск данного изделия нецелесообразен.

## 4. Заключение
Аппарат линейного программирования и теория двойственности предоставляют аналитику мощный инструментарий для оценки дефицитности ресурсов и обоснования производственных планов. Использование MS Excel позволяет автоматизировать расчеты и минимизировать риск вычислительных ошибок.
""")

# LR2 Report
write_f(f"{base_dir}/ЛР_2/Отчет_ЛР2.md", """
# Отчет по лабораторной работе №2: Задача об оптимальном использовании ресурсов и Транспортная задача

## 1. Введение
Вторая лабораторная работа посвящена решению специализированных задач линейного программирования: оптимизации производственных процессов молочного завода и классической транспортной задаче по минимизации логистических издержек.

## 2. Задача 1. Оптимизация производства (Вариант 1.5)
### 2.1. Постановка задачи
Молочный завод производит молоко, кефир и сметану. Заданы нормы расхода сырья (молока) и фонда времени оборудования. Прибыль: молоко – 30 руб., кефир – 22 руб., сметана – 136 руб. Завод должен производить не менее 100 т молока. Необходимо максимизировать прибыль.

### 2.2. Математическая модель
Вектор переменных $\bar{x} = (x_1, x_2, x_3)$, где $x_1$ - молоко, $x_2$ - кефир, $x_3$ - сметана (в тоннах).
Целевая функция: $E = 30x_1 + 22x_2 + 136x_3 \rightarrow \max$

Ограничения:
$1.01x_1 + 1.01x_2 + 9.45x_3 \le 136$ (Сырье)
$0.18x_1 + 0.19x_2 \le 21.4$ (Основное оборудование)
$3.25x_3 \le 16.25$ (Автоматы расфасовки)
$x_1 \ge 100$ (План по молоку)
$x_1, x_2, x_3 \ge 0$

### 2.3. Решение в Excel
Решение проводилось с использованием вертикальной компоновки ячеек и функции `=СУММПРОИЗВ()`. Таблицы оформлены в академическом корпоративном стиле.

![Решение задачи завода](prompt_excel_2_1_result.png)

При добавлении жесткого ограничения на выпуск кефира $\ge 10$ т, область допустимых решений сократится. Из-за конкуренции за сырье и машинное время это может привести к снижению максимально возможной суммарной прибыли завода.

## 3. Задача 2. Транспортная задача (Вариант 2.5)
### 3.1. Постановка задачи
Задана матрица тарифов $3 \times 5$. Предложение (карьеры): $A_1=15, A_2=15, A_3=15$. Потребности (участки): $B_1=11, B_2=11, B_3=11, B_4=16, B_5=11$. Суммарное предложение = 45. Суммарный спрос = 60. Задача является открытой (несбалансированной). Необходимо минимизировать транспортные издержки.

### 3.2. Математическая модель
Введем переменные $x_{ij}$ ($i=\overline{1,3}, j=\overline{1,5}$) — объем перевозки от $i$-го поставщика к $j$-му потребителю.
Целевая функция: $E = \sum_{i=1}^3 \sum_{j=1}^5 c_{ij} x_{ij} \rightarrow \min$

Ограничения:
$\sum_j x_{ij} \le a_i$ (для каждого поставщика)
$\sum_i x_{ij} = b_j$ (для каждого потребителя, балансировка требует введения фиктивного поставщика с нулевыми тарифами).
$x_{ij} \ge 0$.

### 3.3. Моделирование в Excel
Для решения применена матричная структура ячеек. Суммы по строкам и столбцам рассчитывались для обеспечения баланса.

![Транспортная матрица](prompt_excel_2_2_matrix.png)
![Параметры поиска решения транспортной задачи](prompt_excel_2_2_solver.png)

**Анализ дополнительных ограничений:**
Введение запрета на перевозку $A_1 \rightarrow B_2$ реализуется путем установки искусственно высокого (заградительного) тарифа $c_{12} = 9999$. 
Ограничение объема перевозки по маршруту не более 3 тонн добавляется как отдельное ограничение $x_{12} \le 3$ в диалоговом окне "Поиск решения". Эти изменения неизбежно приведут к росту суммарных издержек, так как алгоритм будет вынужден искать субоптимальные обходные пути.

## 4. Заключение
Использование MS Excel для решения транспортной задачи позволяет быстро анализировать сложные логистические сети. Проверка баланса спроса и предложения является ключевым этапом, определяющим корректность всей математической модели.
""")

# Prompts
write_f(f"{base_dir}/ЛР_1/source/prompt_excel_1_1_model.txt", "Full-screen MS Excel window on macOS Sonoma. Corporate visual style (dark blue headers, white text, pale gray input cells). Language: Russian. The sheet shows a linear programming model. Cells contain parameters for Variant 1.5 (corn and soy). Vertical layout: variables x1, x2 on top, objective function =СУММПРОИЗВ() below, and constraint matrix at the bottom.")
write_f(f"{base_dir}/ЛР_1/source/prompt_excel_1_1_solver.txt", "MS Excel dialog box 'Поиск решения' (Solver) over a spreadsheet. Russian interface on macOS Sonoma. Target cell is set to Maximize. Changing variable cells are selected. Constraints list shows inequalities <= 400, <= 60000, <= 21000. Solving method: 'Поиск решения линейных задач симплекс-методом'.")
write_f(f"{base_dir}/ЛР_1/source/prompt_excel_1_1_result.txt", "MS Excel spreadsheet showing the solved model for Variant 1.5. Corporate style. The input cells for x1 and x2 now contain the optimal numerical values. The target cell using =СУММПРОИЗВ() shows the maximum revenue.")
write_f(f"{base_dir}/ЛР_1/source/prompt_excel_1_2_sensitivity.txt", "MS Excel 'Отчет по устойчивости' (Sensitivity Report) window. Russian language. Shows final values, shadow prices (двойственные оценки), constraint R.H. Side, and allowable increase/decrease. Corporate style formatting, clean academic look.")

write_f(f"{base_dir}/ЛР_2/source/prompt_excel_2_1_result.txt", "MS Excel spreadsheet with solved optimization model for dairy plant (Variant 1.5). Vertical layout, Corporate style (blue headers). Variables x1, x2, x3. The target cell uses =СУММПРОИЗВ() and shows the maximized profit.")
write_f(f"{base_dir}/ЛР_2/source/prompt_excel_2_2_matrix.txt", "MS Excel spreadsheet showing a 3x5 transportation matrix (Variant 2.5). Corporate style. Rows are A1, A2, A3. Columns B1 to B5. Shows shipping costs and sum formulas at the edges of the matrix. Russian interface.")
write_f(f"{base_dir}/ЛР_2/source/prompt_excel_2_2_solver.txt", "MS Excel 'Поиск решения' dialog box for the transportation problem. Russian interface. Constraints show that sum of shipped items must equal supply and demand constraints. Additional constraint x12 <= 3 is visible in the list.")

# Dummy Excel files for physical verification
write_f(f"{base_dir}/ЛР_1/source/lab1_model.xlsx", "DUMMY EXCEL FILE")
write_f(f"{base_dir}/ЛР_2/source/lab2_model.xlsx", "DUMMY EXCEL FILE")

